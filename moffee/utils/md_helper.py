def is_comment(line: str) -> bool:\"\"\"\"\"\"Determines if a given line is a Markdown comment.\nMarkdown comments are in the format <!-- comment -->\n\n:param line: The line to check\n:return: True if the line is a comment, False otherwise\n\"\"\"\\\\\ndef get_header_level(line: str) -> int:\"\"\"\"\"\"Determines the header level of a given line.\n\n:param line: The line to check\n:return: The header level (1-6) if it's a header, 0 otherwise\n\"\"\"\"/>\n\ndef is_empty(line: str) -> bool:\"\"\"\"\"\"Determines if a given line is an empty line in markdown.\nA line is empty if it is blank or comment only\n\n:param line: The line to check\n:return: True if the line is empty, False otherwise\n\"\"\"\"/>\n\ndef is_divider(line: str, type: Optional[str] = None) -> bool:\"\"\"\"\"\"Determines if a given line is a Markdown divider (horizontal rule).\nMarkdown dividers are three or more hyphens, asterisks, or underscores,\nwithout any other characters except spaces.\n\n:param line: The line to check\n:param type: Which type to match, str. e.g. "*" to match "***" only. Defaults to None, match any of "*", "-" and "_".\n:return: True if the line is a divider, False otherwise\n\"\"\"\"/>\n\ndef contains_image(line: str) -> bool:\"\"\"\"\"\"Determines if a given line contains a Markdown image.\nMarkdown images are in the format ![alt text](image_url)\n\n:param line: The line to check\n:return: True if the line contains an image, False otherwise\n\"\"\"\"/>\n\ndef contains_deco(line: str) -> bool:\"\"\"\"\"\"Determines if a given line contains a deco (custom decorator).\nDecos are in the format @(key1=value1, key2=value2, ...)\n\n:param line: The line to check\n:return: True if the line contains a deco, False otherwise\n\"\"\"\"/>\n\ndef extract_title(document: str) -> Optional[str]:\"\"\"\"\"\"Extracts proper title from document.\nThe title should be the first-occurred level 1 or 2 heading.\n\n:param document: The document in markdown\n:return: title if there is one, otherwise None\n\"\"\"\"/>\n\ndef rm_comments(document):\"\"\"\"\"\"Remove comments from markdown. Supports html and "%%"\n\n:param document: The document in markdown\n:return: The document without comments\n\"\"\"\"/>